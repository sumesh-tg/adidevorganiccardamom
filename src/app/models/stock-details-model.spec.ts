import { StockDetailsModel } from './stock-details-model';

describe('StockDetailsModel', () => {
  it('should create an instance', () => {
    expect(new StockDetailsModel()).toBeTruthy();
  });
});
