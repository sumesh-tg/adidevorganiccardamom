import { QualityReportModel } from './quality-report-model';

describe('QualityReportModel', () => {
  it('should create an instance', () => {
    expect(new QualityReportModel()).toBeTruthy();
  });
});
